# This file is needed to get Qt to load its plugins from this installation
# directory instead of the hard-coded path from the library.  However,
# the defaults with this file present are suitable without any variables
# specified.

[Paths]
# Prefix = /home/user/snt/qualnet/5.0/gui/lib/linux64
# Plugins = plugins
